<?xml version="1.0" encoding="UTF-8" ?>
<toolkit:BaseView x:Class="XLabs.Samples.Pages.Services.GeolocatorPage"
                  xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:toolkit="clr-namespace:XLabs.Forms.Mvvm;assembly=XLabs.Forms"
                  Title="Geolocator">
    <StackLayout>
        <Button Command="{Binding GetPositionCommand}" Text="Get Position" />
        <Entry Text="{Binding PositionStatus}" />
        <Entry Text="{Binding PositionLatitude}" />
        <Entry Text="{Binding PositionLongitude}" />
        <Button Command="{Binding SendPositionEmail}" Text="Send Position Email" />
        <Button Command="{Binding SendPositionSms}" Text="Send Position SMS" />
        <Button Command="{Binding OpenPositionUri}" Text="Open Position URI" />
        <Button Command="{Binding GetDrivingDirections}" Text="Driving Directions" />
    </StackLayout>
</toolkit:BaseView>

